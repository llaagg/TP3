@page "/"

@inject TP3Client TP3Client

<pre class="code">
TP3 - 
    Three Plus 3
    Tree plus 3
</pre>

<button class="btn btn-primary" @onclick="OnClick">Connect</button>

@code
{
    private async Task OnClick()
    {
        try{
            await TP3Client.Connect();
            var responses = await TP3Client.ListenForResponses();

        }catch(Exception ex)
        {
            Console.WriteLine($"Error connecting to IPC server: {ex.Message}");
        }
    }
}